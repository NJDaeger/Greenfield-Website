{
  "name": "gatsby-playground-repo",
  "version": "1.0.0",
  "private": true,
  "description": "gatsby-playground-repo",
  "author": "NJDaeger",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "preload": "gatsby-preload-fonts",
    "deploy": "gatsby clean && npm run preload && gatsby build --prefix-paths && gh-pages -d public"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "gatsby": "^4.0.0",
    "gatsby-plugin-preload-fonts": "^3.4.0",
    "gatsby-plugin-sass": "^5.0.0",
    "gatsby-plugin-sitemap": "^5.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "sass": "^1.43.3"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
